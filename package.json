{
  "name": "meme-radar",
  "version": "0.1.0",
  "private": true,
  "description": "Terminal intelligence radar for pons v2 launches on Robinhood Chain",
  "type": "module",
  "bin": {
    "meme-radar": "./dist/cli.js"
  },
  "scripts": {
    "dev": "npm run build && node dist/src/cli.js",
    "hunt": "npm run build && node dist/src/cli.js hunt",
    "scan": "npm run build && node dist/src/cli.js scan",
    "doctor": "npm run build && node dist/src/cli.js doctor",
    "demo": "npm run build && node dist/src/cli.js demo",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "viem": "^2.56.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.23.0",
    "typescript": "^5.9.0"
  }
}
